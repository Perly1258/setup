# LangChain v1.x Compatibility & Smart Caching Requirements
# Installation order matters - install in the order listed

# ============================================================================
# STEP 1: Install Open WebUI FIRST (it has strict dependency pinning)
# ============================================================================
open-webui==0.6.43

# ============================================================================
# STEP 2: Install LangChain v1 API-compatible stack
# ============================================================================
# Note: LangChain 0.3.x provides the v1 API (langchain-core 0.3+) while
# maintaining compatibility with AgentExecutor and create_react_agent.
# LangChain 1.x (1.0+) has deprecated these APIs in favor of LangGraph.
langchain>=0.3.0,<1.0.0
langchain-community>=0.3.0,<1.0.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0
langchainhub>=0.1.15

# ============================================================================
# STEP 3: Core dependencies
# ============================================================================
pydantic>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# ============================================================================
# STEP 4: LLM and Embeddings
# ============================================================================
sentence-transformers>=2.2.2
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0

# ============================================================================
# STEP 5: LlamaIndex (compatible with LangChain v1.x)
# ============================================================================
llama-index-core>=0.10.0
llama-index-llms-ollama>=0.1.0
llama-index-embeddings-ollama>=0.1.0
llama-index-vector-stores-postgres>=0.1.0
llama-index-readers-file>=0.1.0
llama-index>=0.10.0

# ============================================================================
# STEP 6: Web framework and utilities
# ============================================================================
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# ============================================================================
# STEP 7: Data processing and visualization
# ============================================================================
pandas>=2.0.0
numpy>=1.24.0
numpy-financial>=1.0.0
matplotlib>=3.7.0
pypdf>=3.15.0
pymupdf>=1.23.0
tabulate>=0.9.0

# ============================================================================
# STEP 8: Development tools
# ============================================================================
ipykernel>=6.25.0
spyder-kernels>=2.4.0
huggingface-hub>=0.16.0

# ============================================================================
# STEP 9: Database and caching (for smart caching layer)
# ============================================================================
redis>=5.0.0  # Optional: for distributed caching
alembic>=1.12.0  # For database migrations
